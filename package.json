{
  "name": "cinevoraces",
  "scripts": {
    "unix-install": "(cd api && npm install); (cd app && npm install)",
    "win-install": "(cd api && npm i) && (cd ../app && npm i)",
    "dev-build": "docker-compose -f docker-compose.dev.yml up --build -d",
    "dev": "docker-compose -f docker-compose.dev.yml up",
    "build-test": "docker-compose -f docker-compose.test.yml up --build -d",
    "build": "docker-compose up --build -d",
    "build-app": "docker exec app npm run build",
    "rebuild-app": "docker-compose down && docker volume rm cinevoraces_next && npm run build && npm run build-app && docker-compose restart", 
    "start": "docker-compose up"
  },
  "dependencies": {
    "app": "file:app",
    "cinevoraces-api": "file:api"
  }
}