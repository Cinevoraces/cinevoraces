{
  "name": "cinevoraces",
  "scripts": {
    "install": "(cd api && npm i) && (cd ../app && npm i)",
    "dev-build": "docker compose up --build",
    "dev": "docker compose up",
    "test": "(cd api && npm run test)"
  },
  "dependencies": {
    "app": "file:app",
    "cinevoraces-api": "file:api"
  }
}
